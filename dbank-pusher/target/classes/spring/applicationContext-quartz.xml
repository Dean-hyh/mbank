<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--========定时任务测试开始========-->
    <bean id="autoDemoTask" class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
        <!--任务类-->
        <property name="jobClass" value="com.ms.credit.quartz.job.AutoJobDemo"/>
        <property name="jobDataAsMap">
            <map>
                <entry key="autoJobDemoService" value="com.ms.credit.quartz.service.AutoJobDemoService"/>
            </map>
        </property>
    </bean>

    <bean id="cornTriggerMyTask" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="autoDemoTask"/>
        <property name="cronExpression" value="${QUARTZ_CRON_MYTASK_SERVICE}"/>
    </bean>
    <!--========定时任务测试结束========-->

    <!-- 总管理容器 -->
    <bean id="startQuartz" class="org.springframework.scheduling.quartz.SchedulerFactoryBean" >
        <property name="triggers">
            <list>
                <ref bean="cornTriggerMyTask"/>
            </list>
        </property>
    </bean>
</beans>
